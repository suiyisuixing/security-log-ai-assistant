# Release Checklist — v3.0-rc

- [x] All sample logs and datasets use RFC 5737 or RFC 1918 IP space.
- [x] No real API keys committed.
- [x] No external HTTP calls in backend.
- [x] No `subprocess` / `os.system` in backend.
- [x] README, CHANGELOG, LICENSE present and updated for v3.
- [x] `pytest` passes locally (284 tests) with no skips/xfails.
- [x] `python -m compileall backend/app tests tools` succeeds.
- [x] `npm run build` succeeds.
- [x] `tools/run_checks.py` returns exit code 0.
- [x] `.gitignore` excludes `.venv`, `node_modules`, `dist`, `.pytest_cache`, etc.
- [x] AI-assisted development disclosure present without naming any LLM vendor.
- [x] CI workflow runs backend tests and frontend build on push/PR to main.
- [x] All v1 + v2 endpoints continue to function (regression-tested).
- [x] All v3 endpoints listed in `/api/surface` (44 endpoints total).
- [x] Path-traversal protection enforced on every endpoint that reads sample logs or datasets.
- [x] No `Authorization` headers leaked by any v3 endpoint.
- [x] All synthetic datasets declare `contains_real_data: false`.
- [x] All playbooks include an educational `limitations` field.
- [x] Detection engineering report contains rule quality, coverage, metrics, FP notes, tuning suggestions, and detection gaps.
